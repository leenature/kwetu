@extends('layouts.marketplace')
@section('title', 'Find a residence')
@section('content')
<div class="residence-page mx-n3 mx-md-n5 mt-n4">
    <section class="residence-hero"><div class="residence-shell"><span class="hero-badge"><i class="bi bi-house-heart"></i> Verified homes and spaces</span><h1>Find a place that feels right.</h1><p>Browse verified vacant apartments, houses and commercial spaces. Compare price, amenities and location before contacting the owner.</p><form class="residence-filter row g-2" method="GET"><div class="col-md-5"><input class="form-control" name="area" value="{{ request('area') }}" placeholder="Search town or county"></div><div class="col-md-4"><select class="form-select" name="type"><option value="">Any property type</option>@foreach($types as $type)<option value="{{ $type }}" @selected(request('type') === $type)>{{ $type }}</option>@endforeach</select></div><div class="col-md-3"><button class="btn btn-primary w-100 h-100"><i class="bi bi-search me-1"></i> Search homes</button></div></form></div></section>
    <section class="residence-shell py-5"><div class="d-flex justify-content-between align-items-center mb-4"><div><h2 class="listing-title mb-1">Available residences</h2><p class="mb-0">{{ $properties->total() }} verified properties with vacancies</p></div><a href="{{ route('home') }}" class="btn btn-outline-primary">Back to Kwetu</a></div><div class="row g-4">@forelse($properties as $property)<div class="col-md-6 col-xl-4"><article class="residence-card"><div class="residence-card-top"><small><i class="bi bi-patch-check-fill"></i> Verified</small><h3 class="mt-2">{{ $property->name }}</h3><span>{{ $property->town }}, {{ $property->county }}</span></div><div class="residence-card-body"><div class="residence-meta mb-3"><span>{{ $property->type }}</span><span><i class="bi bi-door-open"></i> {{ $property->units->count() }} vacant</span></div><div class="residence-price">From KSh {{ number_format($property->units->min('monthly_rent'), 0) }} <small class="fw-normal text-muted">/ month</small></div><p class="small mt-2">{{ \Illuminate\Support\Str::limit($property->description, 100) }}</p><a class="btn btn-primary w-100 mt-2" href="{{ route('residences.show', $property) }}">View residence <i class="bi bi-arrow-right"></i></a></div></article></div>@empty<div class="col-12"><div class="text-center py-5 bg-white rounded-4"><i class="bi bi-search fs-1 text-primary"></i><h3 class="mt-3">No matching residences yet</h3><p>Try a different location or property type.</p></div></div>@endforelse</div><div class="mt-4">{{ $properties->links() }}</div></section>
</div>
@endsection
