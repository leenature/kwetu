<?php
use Illuminate\Database\Migrations\Migration; use Illuminate\Database\Schema\Blueprint; use Illuminate\Support\Facades\Schema; use Illuminate\Support\Facades\DB; use Illuminate\Support\Str;
return new class extends Migration { public function up(): void { Schema::table('tenants', fn (Blueprint $table) => $table->string('portal_token', 64)->nullable()->unique()->after('email')); DB::table('tenants')->orderBy('id')->eachById(function ($tenant) { DB::table('tenants')->where('id', $tenant->id)->update(['portal_token' => Str::random(48)]); }); } public function down(): void { Schema::table('tenants', fn (Blueprint $table) => $table->dropUnique(['portal_token'])->dropColumn('portal_token')); } };
