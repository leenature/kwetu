@extends('layouts.app')
@section('title', 'Settings')
@section('content')
<div class="page-header"><div><h2>Settings</h2><p>Manage your personal access, workspace tools, and security.</p></div></div>
<div class="row g-4"><div class="col-lg-7"><div class="finance-panel p-4"><div class="d-flex align-items-center gap-3 mb-4"><span class="profile-avatar fs-4">{{ strtoupper(substr(auth()->user()->name, 0, 1)) }}</span><div><h5 class="mb-0">{{ auth()->user()->name }}</h5><small>{{ auth()->user()->email }} · {{ auth()->user()->role }}</small></div></div><a class="btn btn-primary" href="{{ route('profile.edit') }}"><i class="bi bi-pencil-square me-1"></i> Change my name, email or password</a></div></div><div class="col-lg-5"><div class="finance-panel p-4"><h5>Workspace access</h5><p class="small">Your role determines which modules are available in this workspace.</p><div class="d-flex flex-wrap gap-2">@foreach(['properties'=>'Properties','units'=>'Units','tenants'=>'Tenants','leases'=>'Leases','payments'=>'Payments','expenses'=>'Expenses','reports'=>'Reports'] as $module => $label)<span class="status-pill {{ auth()->user()->canAccessModule($module) ? 'status-paid' : 'status-cancelled' }}"><i class="bi {{ auth()->user()->canAccessModule($module) ? 'bi-check-lg' : 'bi-lock' }}"></i> {{ $label }}</span>@endforeach</div>@if(in_array(auth()->user()->role, ['Owner','Super Admin']))<hr><a href="{{ route('users.index') }}" class="btn btn-outline-primary btn-sm">Manage team roles and access</a>@endif</div></div>@if(auth()->user()->role === 'Super Admin')<div class="col-12"><div class="finance-panel p-4"><h5>Landing page and partners</h5><p class="small">Change the welcome-page message and choose which service partners appear publicly.</p><a href="{{ route('settings.landing') }}" class="btn btn-primary"><i class="bi bi-megaphone me-1"></i> Manage landing page</a></div></div>@endif</div>
@endsection
