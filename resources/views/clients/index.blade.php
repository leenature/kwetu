@extends('layouts.app')
@section('title','Clients')
@section('content')
<div class="container-fluid"><div class="d-flex justify-content-between align-items-center mb-4"><div><h3>Property clients</h3><p class="text-secondary mb-0">Give property owners a private portfolio view.</p></div>@if(in_array(auth()->user()->role,['Owner','Super Admin']))<button class="btn btn-primary" data-bs-toggle="modal" data-bs-target="#clientModal">Add client</button>@endif</div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif<div class="card shadow-sm"><div class="table-responsive"><table class="table table-hover mb-0"><thead><tr><th>Client</th><th>Contact</th><th>Properties</th><th></th></tr></thead><tbody>@forelse($clients as $client)<tr><td><strong>{{ $client->name }}</strong></td><td>{{ $client->phone }}<small class="d-block text-secondary">{{ $client->email }}</small></td><td>{{ $client->properties_count }}</td><td><a class="btn btn-sm btn-outline-primary" target="_blank" href="{{ route('client-portal.show',$client->portal_token) }}">Open portal</a></td></tr>@empty<tr><td colspan="4" class="text-center py-5 text-secondary">No clients yet.</td></tr>@endforelse</tbody></table></div></div>{{ $clients->links() }}</div>
@if(in_array(auth()->user()->role,['Owner','Super Admin']))<div class="modal fade" id="clientModal" tabindex="-1"><div class="modal-dialog"><form method="POST" action="{{ route('clients.store') }}" class="modal-content">@csrf<div class="modal-header"><h5 class="modal-title">Add client</h5><button type="button" class="btn-close" data-bs-dismiss="modal"></button></div><div class="modal-body"><input name="name" class="form-control mb-3" placeholder="Client name" required><input name="phone" class="form-control mb-3" placeholder="Phone" required><input name="email" type="email" class="form-control mb-3" placeholder="Email"><textarea name="address" class="form-control" placeholder="Address"></textarea></div><div class="modal-footer"><button class="btn btn-primary">Save client</button></div></form></div></div>@endif
@endsection
