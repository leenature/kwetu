@extends('layouts.app')
@section('title','Bulk onboarding')
@section('content')
<div class="container-fluid"><div class="d-flex justify-content-between align-items-center mb-4"><div><h3>Bulk onboarding</h3><p class="text-secondary mb-0">Bring an existing portfolio into Kwetu in one CSV upload.</p></div><a class="btn btn-outline-primary" href="{{ route('onboarding.template') }}"><i class="bi bi-download me-1"></i> Download CSV template</a></div>@if(session('success'))<div class="alert alert-success">{{ session('success') }}</div>@endif @if($errors->any())<div class="alert alert-danger">{{ $errors->first() }}</div>@endif<div class="card shadow-sm"><div class="card-body"><h5>How it works</h5><ol><li>Download the template and open it in Excel.</li><li>Add one row for each unit. Use <code>Occupied</code> plus tenant details to create an active tenant and lease.</li><li>Save as CSV UTF-8 and upload it here.</li></ol><form method="POST" action="{{ route('onboarding.store') }}" enctype="multipart/form-data" class="row g-3"><div class="col-md-8"><input type="file" name="file" accept=".csv,text/csv" class="form-control" required></div><div class="col-md-4"><button class="btn btn-primary w-100">Import portfolio</button></div></form></div></div></div>
@endsection
